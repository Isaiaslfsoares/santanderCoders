
----------------------------             HTML                ----------------------------------- 
HTML - Estrutura do site 
html - head - body - main - footer - script - style
tags 
div
input
p
body

----------------------------        CSS - Estilização        ----------------------------------- 
margin
padding
background-color
display
flex-direction
align-items
color
font-family
width
height


----------------------------            JavaScript           ----------------------------------- 

Variáveis - var, let e const 
Tipos: string, number, boolean, array, objeto, 
function 
if, if else, < > = && || !

Funções:
isNaN() - Verifica se é um numero, booleano. 
.push() - insere o elemento novo como ultimo elemento do array
Math.random() - cria um número aleatório entre 0 e 1. Math.floor((Math.random()*max-min)+mín)
Math.floor() - arredonda o valor para baixo.
splice() - retira e adiciona elementos em um array. recebe 3 parâmetros (indiceInicial, quantosCortes, add)
parseInt() - transforma em inteiro retirando as casas decimais
parseFloor() - transforma string em numero flutuante

onclick('') recebe uma função como parâmetro que é executa ao ser clicado
setTimeout

DOM:
.document.getElementById('idDoItem') Leva um elemento do HTML para o SCRIPT pelo ID do elemento.
.addEventListener('click/evento', function(/parametro){/blocodecódigo}) - Observa um evento que ocorre no HTML e executa uma função quando ocorrer
.document.createElement("tag"); Cria uma tag específica
.innerText - altera ou leva o texto ao elemento selecionado.
.innetHTML - mesma coisa, mas interpreta as tags de html
.append() - adiciona um novo elemento ao HTML **estudar**
.value - verifica o valor, usado quando o input é textual

return; // encerra aplicação no if

1 - html CSS
2 - criar função usando chamadas que já fizemos varias vezes
3 - condicionais
4 - loops e fors
5 - loop e fors
6 - cabulosa que veremos hj - setTimeout (funcao, tempo)